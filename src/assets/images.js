import logoPng from './logo.png';
import loginBgPng from './login-bg.png';

const logo = logoPng;
const loginBackground = loginBgPng;

const images = {
  logo,
  loginBackground
};

export default images;